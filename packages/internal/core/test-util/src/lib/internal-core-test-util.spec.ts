import { internalCoreTestUtil } from './internal-core-test-util';

describe('internalCoreTestUtil', () => {
  it('should work', () => {
    expect(internalCoreTestUtil()).toEqual('internal-core-test-util');
  });
});
